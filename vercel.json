{
  "version": 2,
  "buildCommand": "npm install --legacy-peer-deps && npm install react-app-rewired --save-dev && npm run build",
  "outputDirectory": "build",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "github": {
    "silent": true
  }
} 